<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:cal="http://www.caliburnproject.org"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:dgx="urn:tom-englert.de/DataGridExtensions" mc:Ignorable="d" x:Class="SCaddins.ExportManager.Views.SCexportView"
    d:DesignWidth="628" d:DesignHeight="272">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="0.80*" />
            <ColumnDefinition
                Width="0.10*" />
            <ColumnDefinition
                Width="0.10*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="30" />
            <RowDefinition
                Height="*" />
            <RowDefinition
                Height="30" />
        </Grid.RowDefinitions>
        <ComboBox
            ItemsSource="{Binding ViewSheetSets}"
            SelectedItem="{Binding SelectedViewSheetSet}"
            Width="Auto"
            Height="Auto"
            Grid.Column="0"
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Margin="5,0,5,5"
            x:Name="ViewsetCombo"
            Grid.ColumnSpan="2" />
        <DataGrid
            x:Name="Sheets"
            SelectionMode="Extended"
            cal:Message.Attach="[Event SelectionChanged] = [Row_SelectionChanged($eventArgs)]"
            AutoGenerateColumns="False"
            Grid.Column="0"
            Grid.Row="1"
            Width="Auto"
            Height="Auto"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Margin="5,0,5,5"
            Grid.ColumnSpan="3" GridLinesVisibility="Vertical" dgx:DataGridFilter.IsAutoFilterEnabled="True" IsReadOnly="True">
            <DataGrid.VerticalGridLinesBrush>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.WindowFrameColorKey}}"/>
            </DataGrid.VerticalGridLinesBrush>
            <DataGrid.HorizontalGridLinesBrush>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.WindowFrameColorKey}}"/>
            </DataGrid.HorizontalGridLinesBrush>
            <DataGrid.AlternatingRowBackground>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlLightColorKey}}"/>
            </DataGrid.AlternatingRowBackground>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Export Name"
                    Binding="{Binding FullExportName}" />
                <DataGridTextColumn
                    Header="Number"
                    Binding="{Binding SheetNumber}" />
                <DataGridTextColumn
                    Header="Name"
                    Binding="{Binding SheetDescription}" />
                <DataGridTextColumn
                    Header="Revision"
                    Binding="{Binding SheetRevision}"/>
                <DataGridTextColumn
                    Header="Revision Description"
                    Binding="{Binding SheetRevisionDescription}" />
                <DataGridTextColumn
                    Header="Export Directory"
                    Binding="{Binding ExportDirectory}" />
                <DataGridTextColumn
                    Header="Scale"
                    Binding="{Binding Scale, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn
                    Header="North Point"
                    Binding="{Binding NorthPointVisible}" />
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem
                        Header="No View Filter">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/help.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem
                        Header="Filter">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/help.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Options"  cal:Message.Attach="OptionsButton">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/gear.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Open Selected View[s]" cal:Message.Attach="OpenViewsCommand">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/open.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Add Revision" cal:Message.Attach="AddRevision">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/revision.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Copy Sheets" cal:Message.Attach="CopySheets">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/copy.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Remove Underlays From View" cal:Message.Attach="RemoveUnderlays">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/underlay.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Rename Selected Views">
                        <MenuItem.Icon>
                            <Image
                                Source="/SCaddins;component/Assets/rename.png" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Fix Scale Bars" cal:Message.Attach="FixScaleBars"/>
                    <MenuItem Header="North Points On" cal:Message.Attach="TurnNorthPointsOn"/>
                    <MenuItem Header="North Points Off" cal:Message.Attach="TurnNorthPointsOff"/>
                    <MenuItem Header="Create User Views" cal:Message.Attach="CreateUserViews"/>
                    <Separator />
                    <MenuItem x:Name="SelectAll" Header="Select All" Click="SelectAll_Click"/>
                    <MenuItem x:Name="SelectNone" Header="Select None" Click="SelectNone_Click"  />
                    <Separator />
                    <MenuItem Header="VerifySheets" cal:Message.Attach="VerifySheets" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
        <ProgressBar Grid.Column="0" Grid.Row="2" Margin="5,0,5,5">
            
        </ProgressBar>
        <Button
            cal:Message.Attach="Export"
            Content="Export"
            Width="Auto"
            Height="Auto"
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Margin="5,0,5,5"
            Grid.Column="2" />
        <Button
            cal:Message.Attach="Info"
            Content="Print"
            Height="Auto"
            Width="Auto"
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Margin="0,0,5,5"
            RenderTransformOrigin="0.3571,0.5476"
            x:Name="PrintButton"
            Grid.Column="1" />
        <Button
            Height="Auto"
            Width="Auto"
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Margin="0,0,5,5"
            x:Name="OptionsButton"
            Background="Transparent"
            BorderBrush="Transparent"
            Grid.Column="2">
            <Image Source="/SCaddins;component/Assets/gear.png" Stretch="None" />
            </Button>
    </Grid>












</UserControl>
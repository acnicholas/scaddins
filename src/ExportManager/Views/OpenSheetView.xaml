<UserControl x:Class="SCaddins.ExportManager.Views.OpenSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:SCaddins.ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel MinHeight="100">
        <Label HorizontalAlignment="Left" VerticalAlignment="Center">Search Sheets and Views (Begin Typing)</Label>
        <TextBox x:Name="SearchInput"
                 Grid.Row="1"
                 Height="Auto"
                 Margin="5"
                 AutomationProperties.HelpText="Start Typing"
                 cal:Message.Attach="[Event KeyDown] = [Action KeyDown($eventArgs)]"
                 Keyboard.PreviewKeyDown="SearchInput_PreviewKeyDown"></TextBox>
        <ListView x:Name="SearchResults" Height="Auto" Grid.Row="2" Margin="5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Sheet Number" DisplayMemberBinding="{Binding Path=SheetNumber}"/>
                    <GridViewColumn Header="Sheet Name" DisplayMemberBinding="{Binding Path=Name}"/>
                    <GridViewColumn Header="View Type" DisplayMemberBinding="{Binding Path=ViewType}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>

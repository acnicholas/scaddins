<UserControl x:Class="SCaddins.ExportManager.Views.PrinterSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <GroupBox Header="Select Printer">
            <ComboBox x:Name="Printers" Margin="5" VerticalAlignment="Center"></ComboBox>  
        </GroupBox>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button x:Name="Cancel" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Margin="5" MinWidth="50">Cancel</Button>
            <Button x:Name="OK" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Margin="5" MinWidth="50">OK</Button>
        </Grid>
    </Grid>
</UserControl>

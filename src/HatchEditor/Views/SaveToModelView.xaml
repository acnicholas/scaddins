<UserControl x:Class="SCaddins.HatchEditor.Views.SaveToModelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label x:Name="Label" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="5"></Label>
        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="NewPatternName" Margin="5"></TextBox>
        <Button Grid.Row="2" Grid.Column="0" x:Name="Cancel" Margin="5">Cancel</Button>
        <Button Grid.Row="2" Grid.Column="1" x:Name="Save" Margin="5" IsEnabled="{Binding SavingIsEnabled}">Save</Button>
    </Grid>
</UserControl>
